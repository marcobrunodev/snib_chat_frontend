import { createGlobalStyle } from "styled-components";

const Gaps = createGlobalStyle`
:root {
  --gap-smaller: 0.5rem;
  --gap-small: 1rem;
  --gap-medium: 1.5rem;
  --gap-big: 2rem;
  --gap-bigger: 3rem;
}
`

export default Gaps